'use client';

import React from 'react';
import { ResizableNavbar } from '@/components/ui/resizable-navbar';

export const Header: React.FunctionComponent = (): React.ReactNode => {
  return <ResizableNavbar />;
};
